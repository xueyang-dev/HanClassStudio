# Component Registry

The component registry is `courseware/components/registry.json`.

Agents must only use components listed in that registry. Do not invent component names, renderer contracts, or data fields.

Each component's required and optional data fields are determined by the registry. If a component cannot express the desired activity, choose another registered component or leave a design note in `lesson_spec.md`.

