# Sample Agent Response Plan

I will keep the route as `main-generation` and refine the existing blueprint only.

Planned artifact edits:

- Update `lesson_spec.md` with a clearer warm-up, input, controlled practice, and exit ticket sequence.
- Keep `spec_lock.json` route, runtime template, language policy, and component policy unchanged.
- Update `lesson_blueprint.json` to add:
  - a vocabulary flip card activity
  - a sentence builder for “我在学习中文呢”
  - a listen-and-choose check
  - a matching game for word and pinyin
  - a character formation slide for `学`
- Regenerate `interaction_plan.json` from the edited components.
- Regenerate `media_plan.json` from the updated image/audio needs.

I will not edit `courseware/lesson.html` or `exports/`. HanClassStudio should validate, render, run quality, and export after my artifact edits.
